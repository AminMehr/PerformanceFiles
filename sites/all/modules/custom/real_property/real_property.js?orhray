(function ($) {
    Drupal.behaviors.real_property_behaviors = {
        attach: function (context, settings) {
            var $topTotal = $('.total-row.top');
            var $botTotal = $('.total-row.bottom');
           
            // Make the change
            $topTotal.html($botTotal.html());     
        }
    };
 }(jQuery));